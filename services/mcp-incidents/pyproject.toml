[project]
name = "mcp-incidents"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0,<1.0.0",
  "mcp"
]

[project.scripts]
mcp-incidents = "mcp_incidents.main:run_server"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
